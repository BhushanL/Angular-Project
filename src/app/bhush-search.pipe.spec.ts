import { BhushSearchPipe } from './bhush-search.pipe';

describe('BhushSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BhushSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
